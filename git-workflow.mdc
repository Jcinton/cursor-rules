---
alwaysApply: false
---

# Git 工作流规范

> **重要提示**: 本项目要求所有 Git 提交信息（Commit Message）必须使用中文描述，Type 类型使用英文小写，以保持团队协作的一致性。

## ⚠️ AI 辅助开发提交规范（重要）

**在使用 AI 辅助工具（如 Cursor、GitHub Copilot）进行开发时：**

### 禁止自动提交代码

1. **❌ 不要在未经明确指示的情况下自动提交代码**
   - AI 工具不应该自动执行 `git add` 和 `git commit`
   - 避免在完成代码修改后立即提交

2. **✅ 只有在用户明确要求时才提交代码**
   - 用户明确说"提交代码"、"提交到 git"、"commit"等指令
   - 用户要求"保存并提交"、"完成并提交"等
   - 用户询问"如何提交"时，提供提交命令但不执行

3. **推荐的工作流程**
   - 完成代码修改后，先让用户检查和测试
   - 等待用户确认代码无误后，再询问是否需要提交
   - 如需提交，询问提交信息并使用文件方式提交

4. **提交前的确认**
   - 向用户说明将要提交的文件
   - 确认提交信息是否正确
   - 征得用户同意后再执行提交

### 示例对话

**❌ 错误做法：**
```
AI: 我已经完成了用户登录功能的开发，现在提交代码...
[自动执行 git commit]
```

**✅ 正确做法：**
```
AI: 我已经完成了用户登录功能的开发。
    代码已修改完成，您可以先测试一下。
    如需提交到 Git，请告诉我提交信息。

用户: 提交代码，提交信息是"添加用户登录功能"

AI: 好的，我将使用以下提交信息提交代码：
    feat(用户): 添加用户登录功能
    
    是否继续？
```

## 分支管理规范

### 分支命名规则

```bash
# 主分支
main/master          # 生产环境分支
develop             # 开发环境分支

# 功能分支
feature/user-login   # 功能开发

# 修复分支
bugfix/fix-login-error    # Bug 修复
hotfix/critical-security  # 紧急修复

# 发布分支
release/v1.0.0       # 版本发布
```

### 分支命名规范
- 使用小写字母和连字符
- 前缀清晰表明分支类型
- 名称简洁且具有描述性

## Commit 规范

### ⚠️ 中文提交规范（必读）

**本项目强制要求：**
1. ✅ **Type 类型使用英文小写**（如：feat、fix、docs 等）
2. ✅ **Scope 作用域使用中文**（如：用户、订单、支付等）
3. ✅ **Subject 主题描述使用中文**（如：添加用户注册功能）
4. ✅ **Body 详细描述使用中文**
5. ❌ **禁止使用纯英文描述提交内容**
6. ❌ **禁止使用拼音**

**标准格式：**
```
<type>(<scope>): <中文主题描述>

<中文详细说明>

<footer>
```

### Type 类型

```bash
feat:      # 新功能
fix:       # Bug 修复
docs:      # 文档修改
style:     # 代码格式（不影响代码运行）
refactor:  # 重构（既不是新增功能，也不是修复bug）
perf:      # 性能优化
test:      # 测试相关
chore:     # 构建过程或辅助工具的变动
revert:    # 回退
```

### Commit 规范要点

1. **Type 必须小写（使用英文）**
2. **Subject 必须使用中文，简洁明了（不超过 50 字符）**
3. **Body 使用中文详细说明（可选）**
4. **Footer 关联 Issue（可选）**
5. **每次提交只做一件事**
6. **提交前先测试**
7. **统一使用中文描述，保持团队一致性**

### ⚠️ 避免中文乱码的提交方法（推荐）

**Windows PowerShell 环境下，推荐使用文件方式提交以避免编码问题：**

```bash
# 方法1：使用临时文件提交（推荐）
echo "feat(用户): 添加用户登录功能" | Out-File -Encoding UTF8 .git_commit_msg.txt
git add .
git commit -F .git_commit_msg.txt
rm .git_commit_msg.txt

# 方法2：包含详细说明的提交
@"
feat(用户): 添加用户登录功能

- 实现用户名密码验证
- 添加 JWT Token 生成
- 添加登录日志记录

Closes #123
"@ | Out-File -Encoding UTF8 .git_commit_msg.txt
git commit -F .git_commit_msg.txt
rm .git_commit_msg.txt

# 方法3：使用 PowerShell 函数（一次性配置）
function gitc {
    param([string]$message)
    $message | Out-File -Encoding UTF8 .git_commit_msg.txt
    git commit -F .git_commit_msg.txt
    rm .git_commit_msg.txt
}

# 使用函数提交
git add .
gitc "feat(用户): 添加用户登录功能"
```

**为什么要用文件方式？**
- PowerShell 默认使用 GBK 编码（代码页 936）
- 直接使用 `git commit -m` 会导致中文乱码
- 使用 UTF-8 文件可以保证编码正确
- 提交后自动删除临时文件，保持工作区清洁

## Git 工作流程

### 1. 开发新功能

```bash
# 1. 从 develop 创建功能分支
git checkout develop
git pull origin develop
git checkout -b feature/user-login

# 2. 开发功能，定期提交（使用文件方式避免乱码）
git add .
echo "feat(用户): 实现用户登录接口" | Out-File -Encoding UTF8 .git_commit_msg.txt
git commit -F .git_commit_msg.txt
rm .git_commit_msg.txt

# 3. 推送到远程
git push origin feature/user-login

# 4. 创建 Pull Request
# 在 GitHub/GitLab 上创建 PR，等待代码审查

# 5. 合并后删除分支
git checkout develop
git pull origin develop
git branch -d feature/user-login
```

### 2. 修复 Bug

```bash
# 从 develop 创建修复分支
git checkout develop
git pull origin develop
git checkout -b bugfix/fix-login-error

# 修复 bug 并提交（使用文件方式）
git add .
echo "fix(用户): 修复登录验证错误" | Out-File -Encoding UTF8 .git_commit_msg.txt
git commit -F .git_commit_msg.txt
rm .git_commit_msg.txt

# 推送并创建 PR
git push origin bugfix/fix-login-error
```

### 3. 紧急修复（Hotfix）

```bash
# 从 main 创建 hotfix 分支
git checkout main
git pull origin main
git checkout -b hotfix/critical-security

# 修复并提交（使用文件方式）
git add .
echo "fix(安全): 修复严重安全漏洞" | Out-File -Encoding UTF8 .git_commit_msg.txt
git commit -F .git_commit_msg.txt
rm .git_commit_msg.txt

# 合并到 main
git checkout main
git merge --no-ff hotfix/critical-security
git push origin main

# 合并到 develop
git checkout develop
git merge --no-ff hotfix/critical-security
git push origin develop

# 打标签
git tag -a v1.0.1 -m "紧急修复版本"
git push origin v1.0.1
```

## 常用 Git 命令

### 基本操作

```bash
git status                  # 查看状态
git diff                   # 查看修改
git add <file>             # 添加文件
git commit -m "message"    # 提交
git log                    # 查看日志
```

### 分支操作

```bash
git branch                 # 查看分支
git checkout -b <name>     # 创建并切换分支
git branch -d <name>       # 删除分支
git merge <branch>         # 合并分支
```

### 同步操作

```bash
git pull origin <branch>   # 拉取更新
git push origin <branch>   # 推送
git fetch origin           # 获取远程更新
```

### 撤销操作

```bash
git checkout -- <file>     # 撤销工作区修改
git reset HEAD <file>      # 撤销暂存区
git reset --soft HEAD~1    # 撤销提交，保留修改
git revert <commit-id>     # 撤销某次提交
```

### 暂存操作

```bash
git stash                  # 暂存当前工作
git stash list             # 查看暂存列表
git stash pop              # 弹出暂存
git stash clear            # 清空所有暂存
```

## Git 配置

### 全局配置

```bash
# 配置用户信息
git config --global user.name "Your Name"
git config --global user.email "your.email@example.com"

# 配置别名
git config --global alias.co checkout
git config --global alias.br branch
git config --global alias.ci commit
git config --global alias.st status
```

### 中文编码配置（重要！）

```bash
# 设置提交信息编码为 UTF-8
git config --global i18n.commitencoding utf-8

# 设置日志输出编码为 UTF-8
git config --global i18n.logoutputencoding utf-8

# 禁用路径引用，正确显示中文文件名
git config --global core.quotepath false

# 设置 Git GUI 编码
git config --global gui.encoding utf-8
```

## Git 协作规范

### Pull Request (PR) 规范

**PR 标题**:
```
feat: 添加用户登录功能
fix: 修复订单金额计算错误
```

**PR 描述**:
```markdown
## 功能描述
实现了用户登录功能，包括用户名密码验证和 JWT Token 生成

## 改动内容
- 添加用户登录接口
- 实现 JWT Token 生成和验证
- 添加登录日志记录

## 测试
- [x] 单元测试通过
- [x] 集成测试通过
- [x] 手动测试通过

## 相关 Issue
Closes #123
```

### Code Review 规范

**审查要点**:
- ✅ 代码符合团队规范
- ✅ 没有明显 Bug
- ✅ 代码可读性好
- ✅ 有必要的注释
- ✅ 单元测试覆盖
- ✅ 性能考虑合理

## Git 最佳实践

### 1. 提交频率
- ✅ 每完成一个小功能就提交
- ✅ 每修复一个 Bug 就提交
- ❌ 避免一次提交包含多个不相关的修改

### 2. 提交内容
- ✅ 每次提交只做一件事
- ✅ 提交前先测试
- ✅ 提交前检查代码格式
- ❌ 不要提交未完成的功能

### 3. 分支管理
- ✅ 及时删除已合并的分支
- ✅ 定期同步 develop 分支
- ✅ 功能开发完成后立即合并
- ❌ 不要长期不合并的分支

### 4. 冲突处理
- ✅ 及时拉取最新代码
- ✅ 小心解决冲突
- ✅ 解决冲突后充分测试
- ❌ 不要盲目选择保留或覆盖

### 5. 代码审查
- ✅ 所有代码必须经过 Code Review
- ✅ 至少一人 Approve 才能合并
- ✅ 认真对待审查意见
- ❌ 不要直接推送到 main/develop

## 版本号规范

### 语义化版本（Semantic Versioning）

```
版本格式: MAJOR.MINOR.PATCH

例如: 1.2.3
  1: 主版本号（不兼容的 API 修改）
  2: 次版本号（向下兼容的功能性新增）
  3: 修订号（向下兼容的问题修正）
```

## 团队协作检查清单

### 提交前检查
- [ ] 代码已格式化
- [ ] 单元测试通过
- [ ] 没有 console.log/System.out
- [ ] 没有注释掉的代码
- [ ] Commit 消息符合规范
- [ ] 没有敏感信息

### 合并前检查
- [ ] 已同步最新代码
- [ ] 通过 Code Review
- [ ] CI/CD 构建成功
- [ ] 没有冲突
- [ ] 功能测试通过

### 发布前检查
- [ ] 版本号已更新
- [ ] CHANGELOG 已更新
- [ ] 文档已更新
- [ ] 数据库脚本已准备
- [ ] 回滚方案已准备
