---
alwaysApply: true
---

# Spring Boot 开发规则

## 项目结构

标准分层结构：
```
src/main/java/com/company/project/
├── controller/      # 控制器层
├── service/        # 服务层
│   └── impl/       # 服务实现
├── mapper/         # 数据访问层
├── entity/         # 实体类
├── dto/            # 数据传输对象
├── vo/             # 视图对象
├── config/         # 配置类
├── exception/      # 自定义异常
├── enums/          # 枚举类
├── utils/          # 工具类
└── constant/       # 常量类
```

## Controller 规范

### 基本结构
- 使用 `@RestController` 而非 `@Controller`
- URL 使用小写字母和连字符
- 使用 `@Valid` 验证请求参数
- 统一返回 `Result<T>` 类型
- 记录关键操作日志

### 规范要点
- 使用 `@RequiredArgsConstructor` 构造器注入
- 使用 `@Slf4j` 记录日志
- RESTful 风格的 URL 设计

## Service 规范

### 接口定义
- 定义业务接口
- 明确方法签名和返回值

### 实现类规范
- 使用 `@Service` 注解
- 使用 `@RequiredArgsConstructor` 构造器注入
- 使用 `@Transactional` 管理事务
- 复杂逻辑拆分为私有方法
- 异常统一抛出，由全局处理器处理

## 配置规范

### application.yml 结构
- 使用 `@spring.profiles.active@` 支持 Maven Profile
- 敏感信息使用环境变量
- 提供默认值
- 按环境区分配置文件

## 统一响应格式

### Result 类结构
- `code`: 响应代码
- `message`: 响应消息
- `data`: 响应数据
- `timestamp`: 时间戳

### 静态方法
- `success(T data)`: 成功响应
- `error(String message)`: 错误响应

## 全局异常处理

### 使用 @RestControllerAdvice
- 处理业务异常 `BusinessException`
- 处理参数校验异常 `MethodArgumentNotValidException`
- 处理系统异常 `Exception`
- 统一返回错误响应格式

## MyBatis-Plus 规范

### Mapper 接口
- 使用 `@Mapper` 注解
- 继承 `BaseMapper<T>`
- 简单查询使用继承的方法
- 复杂查询自定义方法

### 配置
```yaml
mybatis-plus:
  mapper-locations: classpath*:mapper/**/*.xml
  type-aliases-package: com.company.project.entity
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: auto
      logic-delete-field: deleted
```

## 依赖注入规范

### 推荐：构造器注入
- 使用 `@RequiredArgsConstructor` 配合 final 字段
- 代码简洁，便于测试

### 避免：字段注入
- 不推荐使用 `@Autowired` 字段注入

## 事务管理规范

### 事务注解
- `@Transactional(rollbackFor = Exception.class)`: 指定回滚异常
- `@Transactional(readOnly = true)`: 只读事务

## 日志规范

### 日志级别
- **INFO**: 关键业务流程
- **WARN**: 潜在问题
- **ERROR**: 错误信息，需要立即处理
- **DEBUG**: 调试信息（生产环境不输出）

### 日志格式
- 使用占位符 `{}`
- 包含关键上下文信息

## API 设计规范

### RESTful URL 设计
```
GET    /api/v1/users          # 获取用户列表
GET    /api/v1/users/{id}     # 获取用户详情
POST   /api/v1/users          # 创建用户
PUT    /api/v1/users/{id}     # 更新用户
DELETE /api/v1/users/{id}     # 删除用户
```

### 分页查询
- `pageNum`: 页码（默认1）
- `pageSize`: 每页数量（默认10）
- `keyword`: 搜索关键字（可选）

## 最佳实践

1. **使用 Lombok 减少样板代码**
2. **接口和实现分离**
3. **统一响应格式**
4. **全局异常处理**
5. **构造器注入**
6. **合理使用事务**
7. **日志记录关键操作**
8. **参数校验使用 Bean Validation**
9. **敏感信息不要硬编码**
10. **代码要有注释**
