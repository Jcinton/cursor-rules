---
alwaysApply: true
---

# MyBatis-Plus 使用规则

## 实体类规范

### 基本注解
- `@TableName`: 指定表名
- `@TableId`: 主键，`IdType.AUTO` 自增，`IdType.ASSIGN_ID` 雪花算法
- `@TableField`: 字段映射
- `@TableLogic`: 逻辑删除字段

### 自动填充注解
- `@TableField(fill = FieldFill.INSERT)`: 插入时自动填充
- `@TableField(fill = FieldFill.INSERT_UPDATE)`: 插入和更新时自动填充

## Mapper 接口规范

### 继承 BaseMapper
- 自动拥有 CRUD 方法
- 自定义复杂查询方法
- 使用 `@Mapper` 注解

## Service 层规范

### 继承 IService 和 ServiceImpl
- IService 提供批量操作方法
- ServiceImpl 提供默认实现
- 自定义业务方法

## 条件构造器使用

### LambdaQueryWrapper（推荐）

**常用条件方法：**
- `eq`: 等于 =
- `ne`: 不等于 !=
- `gt/ge`: 大于/大于等于
- `lt/le`: 小于/小于等于
- `like`: 模糊查询
- `in`: IN 查询
- `between`: BETWEEN 查询
- `isNull/isNotNull`: 空值判断
- `orderByAsc/orderByDesc`: 排序

## 分页查询

### 使用 Page 对象
- 创建 `Page<T>` 对象
- 传入页码和每页数量
- 调用 `page()` 方法

## 批量操作

### 常用方法
- `saveBatch`: 批量插入
- `updateBatchById`: 批量更新
- `removeByIds`: 批量删除

## 逻辑删除配置

### application.yml
```yaml
mybatis-plus:
  global-config:
    db-config:
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0
```

### 实体类
- 使用 `@TableLogic` 注解标注删除字段

## 自动填充

### 配置自动填充处理器
- 实现 `MetaObjectHandler` 接口
- 重写 `insertFill` 和 `updateFill` 方法
- 使用 `@Component` 注册

### 实体类标注
- `@TableField(fill = FieldFill.INSERT)`: 插入时填充
- `@TableField(fill = FieldFill.INSERT_UPDATE)`: 插入和更新时填充

## 常用查询场景

### 单条查询
- `selectById`: 根据 ID 查询
- `selectOne`: 根据条件查询一条

### 列表查询
- `selectList`: 条件查询列表
- `selectBatchIds`: 批量 ID 查询

### 统计查询
- `selectCount`: 统计总数

### 更新操作
- `updateById`: 根据 ID 更新
- `update`: 条件更新

## 性能优化建议

1. **只查询需要的字段**
   - 使用 `select()` 指定查询字段

2. **使用分页避免一次查询大量数据**
   - 使用 `Page` 对象

3. **批量操作使用 saveBatch/updateBatchById**
   - 指定批次大小

4. **避免 N+1 查询问题**
   - 使用关联查询

5. **合理使用索引**
   - 在常查询字段上建立索引

## 最佳实践

1. **使用 LambdaQueryWrapper 避免硬编码字段名**
2. **复杂查询写 XML，简单查询用 Wrapper**
3. **使用逻辑删除而不是物理删除**
4. **统一使用 Service 层方法，避免直接调用 Mapper**
5. **分页查询使用 MyBatis-Plus 的 Page 对象**
6. **批量操作优先使用 saveBatch/updateBatchById**
7. **合理配置自动填充，减少重复代码**
