---
alwaysApply: true
---

# Java 开发核心规则

## 命名规范

### 类名
- 使用 PascalCase（大驼峰）
- 类名必须是名词
- 例如：`UserService`, `OrderController`, `ProductRepository`

### 方法名
- 使用 camelCase（小驼峰）
- 方法名以动词开头
- 例如：`getUserById()`, `saveOrder()`, `isValid()`

### 变量名
- 使用 camelCase
- 有意义的名称，避免单字母（除循环变量）
- 例如：`userName`, `pageSize`, `orderList`

### 常量名
- 使用 UPPER_SNAKE_CASE（全大写下划线）
- 例如：`MAX_RETRY_TIMES`, `DEFAULT_TIMEOUT`

## 代码规范

### 使用 Lombok 简化代码
```java
@Data
@Builder
@NoArgsConstructor
@AllArgsConstructor
public class User {
    private Long id;
    private String username;
}
```

### 使用构造器注入
```java
// 推荐
@RequiredArgsConstructor
public class UserService {
    private final UserMapper userMapper;
}

// 避免使用字段注入
```

### 异常处理
- 捕获具体异常，避免捕获 Exception
- 使用自定义业务异常
- 在 Service 层抛出异常，Controller 层统一处理

### 日志规范
```java
@Slf4j
public class UserService {
    public void example() {
        log.info("用户登录: username={}", username);
        log.error("操作失败: userId={}", userId, exception);
    }
}
```

## Spring Boot 规范

### Controller 层
- 使用 `@RestController`
- RESTful 风格的 URL
- 使用 `@Valid` 验证参数
- 返回统一的 `Result<T>` 对象

### Service 层
- 接口和实现分离
- 使用 `@Transactional` 管理事务
- 业务逻辑在 Service 层
- 复杂查询条件封装为 DTO

### Mapper 层
- 继承 `BaseMapper<T>`（MyBatis-Plus）
- 简单查询使用 Lambda 表达式
- 复杂查询写 XML

## 最佳实践

1. **使用 Optional 避免空指针**
2. **使用 Stream API 处理集合**
3. **合理使用事务注解**
4. **接口要有 Javadoc 注释**
5. **单元测试覆盖核心逻辑**
