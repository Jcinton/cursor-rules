---
alwaysApply: true
---

# Java 单元测试规则

## 测试框架

### 核心依赖
- JUnit 5
- Mockito
- Spring Boot Test
- AssertJ（可选，更好的断言）

## 测试类命名规范

### 命名规则
- 测试类名 = 被测试类名 + Test
- 测试方法名：`test方法名_场景描述_预期结果`
- 使用 `@DisplayName` 添加中文描述

## 测试类基本结构

### Controller 测试模板
- 使用 `@WebMvcTest`
- 使用 `@MockBean` Mock Service
- 使用 `MockMvc` 模拟请求

### Service 测试模板
- 使用 `@ExtendWith(MockitoExtension.class)`
- 使用 `@Mock` Mock 依赖
- 使用 `@InjectMocks` 注入被测试类

### Mapper 测试模板
- 使用 `@SpringBootTest`
- 使用 `@Transactional` 自动回滚
- 使用 `@Autowired` 注入 Mapper

## 测试用例编写原则

### AAA 模式（推荐）
- **Arrange (Given)**: 准备测试数据和环境
- **Act (When)**: 执行被测试的方法
- **Assert (Then)**: 验证结果

## 断言（Assertions）

### JUnit 5 断言
- `assertEquals`: 相等断言
- `assertTrue/assertFalse`: 布尔断言
- `assertNull/assertNotNull`: 空值断言
- `assertThrows`: 异常断言
- `assertTimeout`: 超时断言
- `assertAll`: 组合断言

### AssertJ 断言（推荐）
- 更流畅的 API
- 更好的可读性
- 更丰富的断言方法

## Mock 使用规范

### Mockito 基本用法
- `@Mock`: 创建 Mock 对象
- `@InjectMocks`: 注入 Mock 对象
- `when().thenReturn()`: 定义 Mock 行为
- `verify()`: 验证方法调用
- `ArgumentCaptor`: 参数捕获

### MockMvc 使用
- `perform()`: 执行请求
- `andExpect()`: 验证响应
- `jsonPath()`: JSON 路径断言

## 测试数据准备

### 使用 Builder 模式
- 链式调用构建测试数据
- 代码简洁清晰

### 使用 @BeforeEach 准备
- 每个测试方法执行前都会执行
- 准备通用测试数据

### 测试数据工厂
- 提取公共方法创建测试数据
- 提高代码复用性

## 测试覆盖场景

### 正常流程测试
- 测试正常情况下的完整流程

### 边界条件测试
- 测试最小边界
- 测试最大边界

### 异常情况测试
- 测试参数为空
- 测试业务异常
- 测试系统异常

## 测试最佳实践

### 1. 测试应该独立
- 每个测试独立运行
- 不依赖其他测试的结果

### 2. 一个测试只验证一个场景
- 单一职责原则
- 测试失败时容易定位问题

### 3. 使用有意义的测试名称
- 清晰描述测试场景
- 使用 `@DisplayName` 添加中文说明

### 4. 避免测试私有方法
- 测试公共接口
- 私有方法通过公共方法间接测试

### 5. Mock 外部依赖
- Mock 数据库、Redis、HTTP 调用等
- 不要 Mock 被测试的类本身

## 测试覆盖率要求

### 覆盖率目标
- **整体覆盖率**: ≥ 80%
- **核心业务逻辑**: ≥ 90%
- **Controller 层**: ≥ 70%
- **Service 层**: ≥ 90%
- **Mapper 层**: ≥ 60%

### 查看覆盖率
```bash
# Maven
mvn clean test jacoco:report

# 报告位置
target/site/jacoco/index.html
```

## 测试模板速查

### Controller 测试
- `@WebMvcTest(XxxController.class)`
- `@MockBean XxxService`
- `mockMvc.perform()`

### Service 测试
- `@ExtendWith(MockitoExtension.class)`
- `@Mock XxxMapper`
- `@InjectMocks XxxServiceImpl`

### Mapper 测试
- `@SpringBootTest`
- `@Transactional`
- `@Autowired XxxMapper`
