---
globs: .java
alwaysApply: false
---
# RESTful API 设计规则

## URL 设计规范

### 基本原则
1. 使用名词，不使用动词
2. 使用复数形式
3. 使用小写字母和连字符
4. 不要使用文件扩展名

### URL 结构
```
https://api.example.com/v1/resources
                      版本  资源名（复数）
```

## HTTP 方法使用

- **GET**: 获取资源
- **POST**: 创建资源
- **PUT**: 完整更新资源
- **PATCH**: 部分更新资源
- **DELETE**: 删除资源

## 状态码使用

### 2xx 成功
- `200 OK`: 请求成功
- `201 Created`: 创建成功
- `204 No Content`: 删除成功，无返回内容

### 4xx 客户端错误
- `400 Bad Request`: 请求参数错误
- `401 Unauthorized`: 未登录
- `403 Forbidden`: 无权限
- `404 Not Found`: 资源不存在
- `409 Conflict`: 资源冲突

### 5xx 服务器错误
- `500 Internal Server Error`: 服务器内部错误
- `503 Service Unavailable`: 服务不可用

## 统一响应格式

### 响应结构
- `code`: 响应代码
- `message`: 响应消息
- `data`: 响应数据
- `timestamp`: 时间戳

## 分页查询

### 请求参数
- `pageNum`: 页码（默认1）
- `pageSize`: 每页数量（默认10）
- `keyword`: 搜索关键字（可选）

### 响应结构
- `records`: 数据列表
- `total`: 总记录数
- `pageNum`: 当前页码
- `pageSize`: 每页数量
- `totalPages`: 总页数

## 参数校验

### 使用 Bean Validation 注解
- `@NotBlank`: 不能为空字符串
- `@Length`: 字符串长度限制
- `@Email`: 邮箱格式
- `@Min/@Max`: 数值范围
- Controller 使用 `@Valid` 触发验证

## 过滤、排序、搜索

### 常用查询参数
- `status`: 状态过滤
- `role`: 角色过滤
- `sort`: 排序字段
- `order`: 排序方向（asc/desc）
- `keyword`: 搜索关键字

## 版本控制

### URL 版本控制（推荐）
- `/api/v1/users`
- `/api/v2/users`

### Header 版本控制
- 使用 `API-Version` 请求头

## JWT 认证

### 请求头格式
```
Authorization: Bearer <token>
```

### 拦截器验证流程
1. 提取 Token
2. 验证 Token 有效性
3. 解析用户信息

## 错误处理

### 自定义业务异常
- 定义 `BusinessException`
- 包含错误码和错误消息

### 全局异常处理
- 使用 `@RestControllerAdvice`
- 处理业务异常、参数校验异常、系统异常
- 统一返回错误响应格式

## API 文档

### 使用 Knife4j
- 配置 Swagger
- Controller 添加 `@Api` 和 `@ApiOperation` 注解
- 自动生成 API 文档

## 最佳实践

1. 使用 RESTful 风格的 URL
2. 使用正确的 HTTP 方法
3. 返回合适的 HTTP 状态码
4. 统一响应格式
5. 参数校验使用 Bean Validation
6. 全局异常处理
7. JWT Token 认证
8. API 版本控制
9. 使用 Knife4j 生成文档
10. 日志记录关键操作
