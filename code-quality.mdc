---
alwaysApply: true
---

# Java 代码质量规则

## 代码格式规范

### 缩进和空格
- 使用 **4 个空格**缩进（不使用 Tab）
- 运算符两侧加空格
- 逗号后加空格
- 方法参数之间加空格

### 大括号
- 左大括号不换行
- 右大括号独占一行
- if/else/for/while 必须使用大括号

### 空行使用
- 类成员变量之间不空行
- 方法之间空一行
- 逻辑块之间空一行

## 注释规范

### 类注释
- 包含类的用途说明
- 标注作者和日期

### 方法注释
- 说明方法功能
- 标注参数、返回值、异常

### 避免的注释
- 废话注释
- 注释掉的代码（应删除）

## 异常处理

### 捕获具体异常
- 避免捕获 Exception
- 针对不同异常类型分别处理

### 不要忽略异常
- 空 catch 块是严重问题
- 至少记录日志

### 使用自定义异常
- 定义业务异常类
- 包含错误码和错误消息

## 空值处理

### 使用 Optional
- 返回可能为空的值使用 Optional
- 使用 ifPresent、orElse、orElseThrow 等方法

### 判空检查
- 字符串使用 StringUtils.isNotBlank
- 集合使用 CollectionUtils.isNotEmpty
- 对象使用 Objects.nonNull

### 避免返回 null
- 集合返回空集合而非 null
- 使用 Optional 包装可能为空的对象

## 集合操作

### 使用 Stream API
- 过滤、映射、统计
- 分组、排序
- 使用方法引用简化代码

### 避免在循环中操作数据库
- 使用批量查询代替循环查询
- 使用批量更新代替循环更新

## 资源关闭

### 使用 try-with-resources
- 自动关闭实现 AutoCloseable 的资源
- 避免手动 finally 块关闭资源

## 常量使用

### 使用常量类
- 定义公共常量
- 按类型分类组织

### 避免魔法值
- 所有字面量都应定义为常量
- 常量名清晰表达含义

## 方法设计

### 单一职责
- 一个方法只做一件事
- 复杂逻辑拆分为多个方法

### 参数不宜过多
- 超过3个参数考虑封装为对象
- 使用 DTO 传递参数

### 方法长度适中
- 建议不超过 50 行
- 过长方法考虑拆分

## 日志规范

### 日志级别
- **DEBUG**: 调试信息
- **INFO**: 关键业务流程
- **WARN**: 警告信息
- **ERROR**: 错误信息

### 日志内容
- 使用占位符而非字符串拼接
- 包含关键上下文信息
- 结构化日志便于查询

## 性能优化

### 使用局部变量
- 避免重复调用相同方法
- 提取到局部变量

### 使用 StringBuilder
- 循环中拼接字符串使用 StringBuilder
- 避免使用 + 拼接

### 懒加载
- 延迟初始化
- 单例模式使用静态内部类

## 代码复用

### 提取公共方法
- 消除重复代码
- 提取为私有方法或工具类

### 使用工具类
- Apache Commons
- Hutool
- Google Guava

## 最佳实践清单

- [ ] 代码格式统一
- [ ] 类和方法都有注释
- [ ] 没有空的 catch 块
- [ ] 使用 Optional 处理空值
- [ ] 使用 Stream API 处理集合
- [ ] 使用 try-with-resources 关闭资源
- [ ] 使用常量代替魔法值
- [ ] 方法职责单一，长度适中
- [ ] 日志记录关键操作
- [ ] 没有重复代码
- [ ] 使用工具类简化操作
